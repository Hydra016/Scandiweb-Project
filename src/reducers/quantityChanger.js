

const quantityReducer =  (state=[], action) => {
    switch(action.type) {
        case 'quantity_dec' :
            
        default :
         return state
        }   
}

export default quantityReducer;